# Docker image

该题目的 docker 镜像过大（27.5 MB），未直接放入 git，请从 GitHub Release 下载：

- 文件：`exit.tar.gz`
- 链接：https://github.com/BITs2Sys/BITs2CTF-2025/releases/download/v2025/exit.tar.gz

加载方式：

```bash
docker load -i "exit.tar.gz"
```
