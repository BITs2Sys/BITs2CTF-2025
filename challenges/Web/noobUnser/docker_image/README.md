# Docker image

该题目的 docker 镜像过大（27.6 MB），未直接放入 git，请从 GitHub Release 下载：

- 文件：`noobUnser.tar.gz`
- 链接：https://github.com/BITs2Sys/BITs2CTF-2025/releases/download/v2025/noobUnser.tar.gz

加载方式：

```bash
docker load -i "noobUnser.tar.gz"
```
